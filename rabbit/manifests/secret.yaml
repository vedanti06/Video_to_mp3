apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secret
stringData:
  # Used by rabbitmq image on first data dir init only; remote "guest" is disabled by default.
  RABBITMQ_DEFAULT_USER: app
  RABBITMQ_DEFAULT_PASS: app-dev-secret
type: Opaque